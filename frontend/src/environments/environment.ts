// Development ortamı — ng serve ile kullanılır
export const environment = {
  production: false,
  apiUrl: '/api',          // proxy.conf.json aracılığıyla localhost:8080'e yönlendirilir
  appName: 'ECommerceHub'
};

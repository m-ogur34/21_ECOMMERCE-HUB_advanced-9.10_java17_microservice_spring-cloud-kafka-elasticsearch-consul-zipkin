// Production ortamı — ng build --configuration production ile kullanılır
export const environment = {
  production: true,
  apiUrl: 'https://api.ecommerce.com',  // Gerçek production URL
  appName: 'ECommerceHub'
};
